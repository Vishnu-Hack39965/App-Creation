match /settings/wifi {
  allow read: if true; // Allows anyone logged in to get the Wi-Fi credentials
  allow write: if request.auth.token.email == "c.commando822@gmail.com";
}

match /settings/wifi {
  // Allows any logged-in user to see the Wi-Fi details
  allow read: if request.auth != null; 
  
  // Only you can update the Wi-Fi details
  allow write: if request.auth != null && request.auth.token.email == "c.commando822@gmail.com";
}
